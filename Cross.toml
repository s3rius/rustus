[target.aarch64-unknown-linux-musl]
pre-build = [
  "dpkg --add-architecture $CROSS_DEB_ARCH",
  "apt-get update -y && apt-get install -y musl musl-tools musl-dev cmake g++",
]

[target.x86_64-unknown-linux-musl]
pre-build = [
  "dpkg --add-architecture $CROSS_DEB_ARCH",
  "apt-get update -y && apt-get install -y musl musl-tools musl-dev cmake g++",
]
